# =============================================================================
# RealCoach.ai Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values
# Required variables are marked with (required)

# -----------------------------------------------------------------------------
# Supabase Configuration (required)
# -----------------------------------------------------------------------------
# Get these from: https://supabase.com/dashboard/project/_/settings/api

VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# -----------------------------------------------------------------------------
# Server-Side LLM API Keys (Vercel Environment Variables)
# -----------------------------------------------------------------------------
# These are NOT used in the client - set them in Vercel Project Settings
# Go to: Vercel Dashboard > Project > Settings > Environment Variables
#
# ANTHROPIC_API_KEY=sk-ant-your-claude-key
# OPENAI_API_KEY=sk-your-openai-key
#
# The client calls /api/llm which proxies requests securely

# -----------------------------------------------------------------------------
# Local Development (optional)
# -----------------------------------------------------------------------------
# For local development with `vercel dev`, you can set these locally:
# ANTHROPIC_API_KEY=sk-ant-your-claude-key
# OPENAI_API_KEY=sk-your-openai-key

# -----------------------------------------------------------------------------
# Notes
# -----------------------------------------------------------------------------
# - VITE_ prefixed variables are exposed to the browser (this is expected)
# - Supabase anon key is safe to expose (RLS protects data)
# - LLM API keys are server-side only (never exposed to browser)
# - The /api/llm proxy handles all LLM requests securely
