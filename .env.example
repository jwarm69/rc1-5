# =============================================================================
# RealCoach.ai Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values
# Required variables are marked with (required)

# -----------------------------------------------------------------------------
# Supabase Configuration (required)
# -----------------------------------------------------------------------------
# Get these from: https://supabase.com/dashboard/project/_/settings/api

VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# -----------------------------------------------------------------------------
# LLM Configuration (optional - enables AI coaching)
# -----------------------------------------------------------------------------
# Without these, the app uses mock responses for development

# Claude (Anthropic) - Primary provider
# Used for: coaching responses, calibration, complex reasoning
VITE_ANTHROPIC_API_KEY=sk-ant-your-claude-key

# OpenAI - Fallback provider
# Used for: simple acknowledgments, action generation
VITE_OPENAI_API_KEY=sk-your-openai-key

# Provider preference (optional, auto-detects based on available keys)
# VITE_LLM_PROVIDER=claude

# -----------------------------------------------------------------------------
# Notes
# -----------------------------------------------------------------------------
# - All VITE_ prefixed variables are exposed to the browser (this is expected)
# - Supabase anon key is safe to expose (RLS protects data)
# - LLM keys will be moved to server-side in Phase 3 (security improvement)
